{{ .SiteLabel }} — Reset your leader password

We received a request to reset the password for {{ .LeaderEmail }}.

Reset your password (link expires in {{ .ExpiryHours }} hours):

{{ .ResetLink }}

If you did not request this, you can ignore this email.
